name: happy_us
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  dough: ^1.0.1
  animated_theme_switcher: ^2.0.1
  google_nav_bar: ^5.0.5
  easy_container: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true